"""
Datasets Application Package.

Manages canonical dataset definitions, update cadences, release schedules,
ingestion modes, revision policies, and service level agreements (SLAs).
"""

default_app_config = "apps.datasets.apps.DatasetsConfig"
